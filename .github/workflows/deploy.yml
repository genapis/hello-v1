name: Deploy Konnect API

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    uses: genapis/kong-konnect-deploy/.github/workflows/deploy.yml@v0.0.1
    with:
      region: eu
    secrets: inherit
    permissions:
      contents: read